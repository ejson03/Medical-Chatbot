<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/headnew.ejs') %>
  <style>
     section.timeline-outer {
  width: 80%;
  margin: 0 auto;
}
     #vertical-timeline {
  font-family: 'Open Sans', sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
     }
  
#content {
  margin-top: 50px;
  text-align: center;
}



h1.header {
  font-size: 50px;
  line-height: 70px;
}
/* Timeline */

.timeline {
  border-left: 4px solid #bbbbbb;
  border-bottom-right-radius: 2px;
  border-top-right-radius: 2px;
  box-shadow: 0 2px 5px 0 rgba(0, 0, 0, 0.16), 0 2px 10px 0 rgba(0, 0, 0, 0.12);
  color: #333;
  margin: 50px auto;
  letter-spacing: 0.5px;
  position: relative;
  line-height: 1.4em;
  padding: 20px;
  list-style: none;
  text-align: left;
}

.timeline h1,
.timeline h2,
.timeline h3 {
  font-size: 1.4em;
  position: relative;
  top: 0;
}

.timeline .step {
  border-bottom: 1px solid rgba(160, 160, 160, 0.2);
  padding-bottom: 15px;
  margin-bottom: 20px;
  position: relative;
}

.timeline .step:last-of-type {
  padding-bottom: 0;
  margin-bottom: 0;
  border: none;
}

.timeline .step:before,
.timeline .step:after {
  position: absolute;
  display: block;
  top: 0;
}

.timeline .step:before {
  left: -180px;
  color: #666666;
  content: attr(data-date);
  text-align: right;
  font-weight: 100;
  font-size: 0.8em;
  min-width: 120px;
}

.timeline .step:after {
  color: #fff;
  box-shadow: 0 0 0 8px #007bc3;
  left: -28px;
  background: #007bc3;
  border-radius: 50%;
  height: 11px;
  width: 11px;
  line-height: 11px;
  content: attr(data-notes-count);
  top: 5px;
}
.timeline .step.completed:after {
  box-shadow: 0 0 0 8px #666666;
  background: #666666; 
}
.timeline .step.edit:after {
  box-shadow: 0 0 0 4px #666666;
  background: #666666; 
}
  .timeline .step.approved:after {
  box-shadow: 0 0 0 8px #007bc3;
  background: #007bc3; 
}
/**/
/*——————————————
Responsive Stuff
———————————————*/

@media (max-width: 945px) {
  .timeline .step::before {
    left: 0.5px;
    top: 20px;
    min-width: 0;
    font-size: 13px;
  }
  .timeline h3 {
    font-size: 16px;
    font-weight: 700;
  }
  .timeline p {
    padding-top: 20px;
  }
  section.lab h3.card-title {
    padding: 5px;
    font-size: 16px
  }
}


  
  .vt-table {
    background: #f5f5f5;
    width: 100%;
    margin-bottom: 0.5em;
    font-size: 1em;
    border-collapse: collapse;
    border-spacing: 0;
  }  
  .vt-table tr {
    -moz-box-sizing: border-box;
    box-sizing: border-box;
    margin: 0;
    padding: 0;
    border: 0;
    font-size: 100%;
    font: inherit;
    vertical-align: baseline;
    text-align: left;
    color: #666;
  }
  .vt-table th {
    font-weight: bold;
	  padding: 0.3em;
	  font-size: 0.85em;
    line-height: 1.4em;
  }
  .vt-table td {
    padding: 0.3em;
	  font-size: 0.85em;
    line-height: 1.4em;
  }
  .vt-table thead .vt-product {
    white-space: nowrap;
    width: 100%;
  }
  .vt-table thead .vt-edited {
    white-space: nowrap;
    min-width: 100px;
  }
  .vt-table thead .vt-change, 
  .vt-table thead .vt-before {
    white-space: nowrap;
    min-width: 80px;
  }
  @media screen and (max-width: 600px) {
    .vt-table {
      margin-bottom: 0;
    }
    .vt-table thead {
      display: none;
    }
    .vt-table tr, .vt-table th, .vt-table td {
      display: block;
      padding: 0;
    }
    .vt-table tr {
      border-bottom: none;
      margin: 0 0 0 0;
      padding: 0.5em;
    }
    .vt-table td {
    padding: 0.25em 0 0.25em 0;
    border-bottom: 1px dotted #ccc;
    text-align: right;
  }
  .vt-table td[data-title]:before {
    content: attr(data-title);
    font-weight: bold;
    display: inline-block;
    float: left;
    text-align: left;
    margin-right: 0.2em;
    font-size: 0.9em;
    min-width: 80px;
  }
  .vt-table td:last-child {
    padding-right: 0;
    border-bottom: 0;
  }
    .vt-table tr {
      border-bottom: 10px solid white;
    }
  }
  

  </style>
</head>
<body class="">
  <div class="wrapper ">
    <%- include('../partials/sidebar.ejs') %>
    <div class="main-panel">
      <%- include('../partials/topbar.ejs') %>
      <div class="content main-container">
         <section id="vertical-timeline" class="timeline-outer">
        <div class="container-fluid container" id="content">
         <div class="row">
            <div class="col s12 m12 l12">
               <ul class="timeline">
                  <% for (const record of records){ %>
                     <% if (record.operation != "TRANSFER") { %>
                     <li class="step approved" data-notes-count="✓" data-date="<%= record.date %>">
                        <h3><%= record.operation %></h3>
                      </li>
                      <%} else { %>
                        <li class="step approved" data-notes-count="✓" data-date="<%= record.date %>">
                           <h3><%= record.operation %></h3>
                           <table class='vt-table' summary=''>
                              <thead>
                                 <tr>
                                    <th class='vt-product '>Doctor</th>
                                 </tr>
                              </thead>
                              <tbody>
                                 <% for (const doctor of record.doctor){ %>
                                    <tr class=''>
                                       <td class='vt-product' data-title='Doctor'><%= doctor %></td>  
                                    </tr>
                                  <% } %>
                              </tbody>
                           </table>
                         </li>
                         <% } %>
                         <% } %>

               </ul>
            </div>
         </div>
          
        </div>
        </section>
      </div>
      <%- include('../partials/footer.ejs') %>
   </div>
 </div>
   
 <%- include('../partials/footerfiles.ejs') %>
</body>

</html>
            